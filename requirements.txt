# Airflow AI Alert System - Event-Driven
# Core dependencies
agno>=0.1.0
httpx>=0.24.0

# Optional dependencies for different LLM providers
# For OpenAI-compatible APIs (default)
openai>=1.0.0

# For Ollama (if using local Ollama)
# ollama>=0.1.0  # Uncomment if using Ollama

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
